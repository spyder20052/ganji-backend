import { defineSms } from '../types';

export const MEDICATIONS = defineSms({
  'medications.dispensed': { fr: 'Ganji : votre ordonnance a été délivrée par {pharmacy}.', en: 'Ganji: your prescription was dispensed by {pharmacy}.' },
});
